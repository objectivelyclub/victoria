<Page
    x:Class="qr_generator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:qr_generator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="1*" />
                <RowDefinition Height="1*"/>
                <RowDefinition Height="4*"/>
            </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Button x:Name="openFileButtton" Content="Open MIDI File" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="0" Click="openFileButton_Click"/>
            <Button x:Name="qrGenerateButton" Content="Generate QR Code" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="1" Click="qrGenerateButton_Click" />
        <TextBlock x:Name="fileNameTextBlock" HorizontalAlignment="Stretch" Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" Text="No file selected." VerticalAlignment="Stretch"/>
    </Grid>
</Page>
